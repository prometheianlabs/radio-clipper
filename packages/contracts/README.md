# packages/contracts

Shared API, event, and domain contracts.

Slice 01 intent:

- centralize types shared across frontend and services
- keep contracts aligned with [API-AND-EVENTS.md](../../API-AND-EVENTS.md)