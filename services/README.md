# services

Runtime services live here, but most implementation is deferred beyond Slice 01.

Current placeholders:

- `control-api`
- `ingest-gateway`
- `transcribe-worker`
- `export-worker`

Slice rule:

- create boundaries now
- implement behavior only in the slice that owns it